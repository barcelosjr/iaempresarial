# ---------------------------------------------------------------------------
# Segredos e credenciais — NUNCA commitar
# ---------------------------------------------------------------------------
.env
.env.local
.token_cache.json
*.pem
*.key

# ---------------------------------------------------------------------------
# Dados locais (DuckDB) e saídas geradas
# ---------------------------------------------------------------------------
*.duckdb
*.duckdb.wal
local_data/warehouse.duckdb
reports/*
!reports/.gitkeep

# Dicionário gerado automaticamente a partir do modelo real (pode conter
# nomes internos de tabelas/medidas). Mantido fora do versionamento por padrão.
dictionary/modelo_semantico.md

# ---------------------------------------------------------------------------
# Extrações e análises com dados reais da empresa — NUNCA commitar.
# Arquivos gerados durante consultas/comparações (CSV e JSONs de análise).
# ---------------------------------------------------------------------------
*.csv
*_analise.json
*_kpis.json
sem_*.json
desp_*.json
manutencao_*.json
*_dados.json
dre_multi_empresa.json
balanco_*.json

# Pasta de trabalho fixa dos scripts de atualização de painel (ano_*.js /
# consol_*.js) — dados brutos intermediários, nunca commitar.
Dashboard financeiro/_scratch/

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
!Dashboard financeiro/build/
dist/
.installed.cfg
*.egg

# Ambientes virtuais
.venv/
venv/
env/
ENV/

# Caches de ferramentas
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.cache/

# ---------------------------------------------------------------------------
# Editores / SO
# ---------------------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Logs do job do COAF — contêm nomes de clientes nas mensagens
logs/

# Configurações locais do Claude Code (permissões específicas da máquina)
.claude/settings.local.json

# Perfil dedicado do Chrome usado na impressão das DMFs (cache local, recriado
# sozinho — ver analysis/dmf_html.py, PERFIL_CHROME)
.chrome_perfil_dmf/
