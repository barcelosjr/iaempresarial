# Autenticação Azure AD
msal>=1.28,<2

# Cliente HTTP para a API REST do Power BI
requests>=2.31,<3

# Carregamento de variáveis de ambiente (.env)
python-dotenv>=1.0,<2

# Leitura de configurações YAML (datasets.yaml, briefing.yaml)
pyyaml>=6.0,<7

# Banco de dados analítico local (complemento ao Power BI)
duckdb>=1.0,<2

# Manipulação de resultados tabulares
pandas>=2.1,<3

# Leitura/escrita de planilhas Excel (ingestão e relatórios)
openpyxl>=3.1,<4

# Servidor MCP (Model Context Protocol) exposto ao Claude
mcp>=1.2,<2

# Testes
pytest>=8.0,<9
